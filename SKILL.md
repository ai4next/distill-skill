---
name: distill
description: |
  蒸馏提炼：把任意原始素材（PDF/长文/访谈transcript/字幕/会议纪要/聊天记录/笔记/网页/代码库）蒸馏成高浓度、可溯源、可复用的结构化认知档案。
  三种预设 schema：人物 / 主题 / 文档，也支持自定义维度。产出是**结构化档案**——可直接阅读，
  也可作为任何下游用途的输入。档案自带信度、出处、矛盾与缺口，可独立交付使用。
  触发词：「蒸馏」「提炼」「萃取」「精炼」「提纯」「distill」「把这堆材料蒸馏一下」「从这些资料里提取要点」「帮我梳理成体系」。
  English triggers: "distill this", "extract the essence", "turn these materials into structured knowledge".
  不适用：只是要一段摘要或翻译（那是摘要不是蒸馏）；素材不足一页；用户要的是直接答案而非档案。
---

# 蒸馏 · 信息提炼术

> 「摘要让你读得更快，蒸馏让你想得更远。」

## 核心理念

**蒸馏不是摘要。** 摘要丢结构，蒸馏留骨架。

一份好的蒸馏档案是一套可运行的认知：
- 这份素材的**骨架**是什么？（核心主张）
- 哪些是**真信念**，哪些只是修辞？（重复次数 + 三重验证）
- 每条信息**有多可信**？（信度四级）
- 哪里**自相矛盾**？（矛盾三类，保留不调和）
- 哪里**不知道**？（缺口标注）

**判定线**：拿蒸馏产物去回答一个素材里没直接出现过、但相关的新问题。摘要答不了，蒸馏能答。

**关键区分**：捕捉的是结构和信度，不是「主要讲了什么」。

---

## 三条铁律

1. **不知道就说不知道** — 缺口必须显式标注。宁可交付诚实标注局限的 60 分档案，也不造看似完美的编造档案。
2. **矛盾保留，绝不调和** — 矛盾是信息不是 Bug。禁止「虽然…但是…」式和稀泥。
3. **每条断言带信度** — 无信度标记的条目视为不合格。D 级（推断）必须显式标注，且占比 <20%。

---

## 参考文件路由

**按需读取，不要预先全读。**

| 触发时机 | 读取 |
|---------|------|
| 需要方法论细节（去水三问/信度四级/三重验证/矛盾处理/蒸馏比） | `references/distillation-framework.md` |
| schema = 人物（或虚构角色） | `references/schema-person.md` |
| schema = 主题（领域/方法论） | `references/schema-topic.md` |
| schema = 文档（论文/书籍/代码库/纪要/聊天记录） | `references/schema-document.md` |
| **要把 N 份档案综合成一份**（跨档案：共识/分歧/断层线） | `references/meta-synthesis.md` |
| 需要写档案、或要了解档案格式契约 | `references/artifact-format.md` |
| Phase 5 独立质检 | `references/quality-scorecard.md` |
| Phase 5.5 对抗精炼 | `references/adversarial-refine.md` |

---

## 执行流程

### Phase 0: 入口分流

#### Step 1: 判定 schema

| 素材/意图 | schema | 维度 |
|-----------|--------|------|
| 关于某个**人**（或虚构角色） | `person` | 著作/对话/表达/他者/决策/时间线 |
| 关于某个**领域、方法论、流派** | `topic` | 领域共识/流派分歧/关键概念/经典案例/争议前沿/演进脉络 |
| **单一文档/语料库/代码库/纪要/聊天记录** | `document` | 论点树/证据链/术语表/隐含假设/内部矛盾/信息缺口 |
| **N 份已有档案 → 综合成一份** | `topic` + 综合模式 | 同 topic，另加 `## 综合` 段 |
| 以上都不贴 | `custom` | 与用户确认 2-8 个维度 |

不确定时**问一句**，不要猜。schema 选错，后面全错。

#### Step 1b: 综合模式（跨档案）

用户说「把这几个人综合一下」「他们分歧在哪」「做个流派对比」时走此路。

- 输入是 **N 份已有档案目录**（不是原始素材）
- **schema 仍是 `topic`**，`sources[]` 存那 N 份档案——不新增 schema（理由见 `meta-synthesis.md` §一）
- **必须先跑来源重叠检测**：`python3 scripts/meta_scan.py <档案...> --out distilled/<slug>/`
  - 若两份档案共享素材（如同一本股东信），它们**不构成独立佐证**——「两人共识」可能只是一份素材数了两次
- 语义判定（是否同一问题、是否真对立、是事实分歧还是价值观分歧）由 agent 完成，脚本只给候选矩阵
- 详见 `references/meta-synthesis.md`

#### Step 2: 素材清点

问用户：「你手上有什么素材？直接丢给我——原始素材比网上搜的质量高得多。」

| 模式 | 触发条件 | 策略 |
|------|---------|------|
| **本地语料优先**（默认） | 用户提供了文件 | 先分析本地素材，网络只补缺失维度 |
| **纯本地语料** | 用户说「只用我给的」或蒸馏非公开材料 | 不做网络搜索 |
| **纯网络搜索** | 用户没提供素材，且对象是公开的 | 全维度网络采集 |

**本地语料的质量优势**：用户手上的一手素材（完整书籍、长访谈原文、内部文档）通常比网络二手转述质量高得多，在信度上直接进 A/B 级。

#### Step 3: 蒸馏档位

完整蒸馏是多 agent + 多轮搜索的长任务，**开跑前必须报量级让用户选档**：

| 档位 | 规模 | 适用 | 成本量级 |
|------|------|------|---------|
| 快速 | 3 个维度，每维度限 5 个来源 | 先试效果 / 素材量小 / 预算敏感 | 约标准档 1/3 |
| 标准（默认） | 全维度完整提炼 | 大多数场景 | 中等 |
| 深度 | 全维度 + 一手素材全量下载 + 证据链保留 | 打算长期复用的精品档案 | 最高 |

用户说「就蒸这个」没有更多信息 → 默认 `person`/`topic`/`document` 自动判定 + 本地语料优先 + 标准档，直接推进。

**确认不阻塞交付**：凡是能给默认值的问题都给默认值。不要把现成的价值卡在提问之后。

#### Step 4: 确认输出位置

默认 `distilled/<slug>/`（用户工作区）。**绝不放进 skill 目录**——skill 必须自包含，档案属于用户数据。

---

### Phase 0.5: 建工作区（调研前就建）

```
distilled/<slug>/
├── DISTILLATE.md      # 主档案（Phase 4 填充）
├── manifest.json      # 素材清单 + 维度状态 + 变更记录
├── research/          # 分维度提炼底稿
├── sources/           # 原始素材（books/ transcripts/ articles/ notes/ chat/ code/ video/ other/）
├── QUALITY.md         # 评分卡（Phase 5 填充，重跑覆盖）
├── REFINE.md          # 对抗精炼补丁候选（Phase 5.5 填充，重跑覆盖）
└── EVALS.jsonl        # 评测历史（追加式，只增不改）
```

**完成检查**：
- [ ] 目录已建
- [ ] `manifest.json` 已初始化（`changes` 写入 `initial` 记录）
- [ ] 用户提供的素材已归入 `sources/` 对应子目录（`python3 scripts/ingest.py`）
- [ ] 若是中国人物/中文主题：信息源策略切换为 B站原始视频 / 小宇宙播客 / 权威中文媒体优先（知乎与微信公众号始终排除）
- [ ] 若是增量模式：已读取现有 `DISTILLATE.md` 与 `manifest.json`，标注哪些维度需刷新

**关键规则**：每个 subagent 必须把提炼结果写入 `research/0X-*.md`。**不落盘的提炼等于没做。**

---

### Phase 1: 素材归集与清洗

#### 本地素材处理方式

| 素材类型 | 处理方式 | 主要覆盖维度 |
|---------|---------|------------|
| 书籍 PDF | 直接阅读提取核心论点 | 著作、表达 |
| 演讲/访谈 transcript | 分析问答模式与即兴反应 | 对话、表达 |
| 视频字幕 SRT/VTT | 同 transcript | 对话、表达 |
| 博客/newsletter 导出 | 提取系统性观点 | 著作、表达 |
| 社交媒体导出 | 分析碎片表达模式 | 表达 |
| 会议纪要 | 提取决策逻辑与分歧 | 决策、隐含假设 |
| 聊天记录 | 提取立场、术语、冲突 | 表达、内部矛盾 |
| 代码库 | 架构决策 + 调用点 + 测试 | 论点树、证据链 |
| 用户整理的笔记 | 作为二手来源交叉参考 | 视内容 |

#### 网络补充（仅缺失维度）

**信息源优先级**：

| 来源类型 | 揭示什么 | 权重 |
|---------|---------|------|
| 用户提供的一手素材 | 完整原文，未经二手过滤 | **最高+** |
| 本人著作 / 原始文档 | 系统性思考 | 最高 |
| 长对话 / 访谈 | 即兴思维过程 | 最高 |
| 实际决策记录 | 真实行为 vs 声称 | 最高 |
| 社交媒体 | 表达风格、即时反应 | 中等 |
| 他人评价 | 外部视角、盲点 | 中等 |
| 二手转述 | 参考但需验证（C 级） | 低 |

**信息源黑名单（永远排除）**：知乎、微信公众号、百度百科/百度知道。中文渠道只接受权威媒体（36氪、极客公园、晚点LatePost、财新、第一财经、虎嗅、少数派、机器之心等）；人物访谈类可用小宇宙/喜马拉雅原始音频、B站原始视频（非搬运号）。

#### 工具辅助

- 素材归集与清点：`python3 scripts/ingest.py <素材路径...> --out distilled/<slug>/`
- 已安装的信息获取 skill（若可用）：`web-article-reader`（精确读全文）、`agent-reach`（多渠道）、`pdf`（读 PDF）

---

### Phase 1.5: 🔴 CHECKPOINT · 素材清点确认

**所有素材归集完成后，暂停展示清点摘要**：

```
┌──────────────┬──────────┬──────────┬──────────────────────────┐
│ 维度          │ 来源数量  │ 信度分布  │ 关键发现                  │
├──────────────┼──────────┼──────────┼──────────────────────────┤
│ 01 著作       │ 12       │ A6 B4 C2 │ 核心主张: ...             │
│ 02 对话       │ 8        │ A3 B4 C1 │ 立场变化: ...             │
│ ...          │          │          │                          │
├──────────────┼──────────┼──────────┼──────────────────────────┤
│ 素材总量      │ 412,000 字│ 一手占比: 62% │ 预估蒸馏比 38:1      │
│ 信息缺口      │ 2 处     │          │ 早期记录稀少、家庭维度缺失 │
└──────────────┴──────────┴──────────┴──────────────────────────┘
```

用 `python3 scripts/distill_report.py distilled/<slug>/` 生成。

用户确认素材质量 OK → 进入 Phase 2。用户觉得某维度不够 → 补素材后再继续。

**这个检查点的意义**：素材质量决定档案上限。垃圾进垃圾出，在这里拦截比在 Phase 5 返工便宜得多。

---

### Phase 2: 分维度提炼（并行 Agent）

按 schema 定义的维度启动并行 subagent，每个负责一个维度。

#### 每个 Agent 的硬性要求

- 提炼结果**必须写入** `research/0X-<dim>.md`
- **每条信息标注信度等级**（A/B/C/D）
- 区分「素材说的」vs「他人说他的」vs「我推断的」
- 发现矛盾**直接记录，不要调和**
- 注明素材出处（文件路径或 URL），便于 Phase 5 溯源抽查

#### 出处与信度的写法（可机械检查）

行尾一对括号写全「信度 + 出处」，中间用 `·` 分隔：

```markdown
- 用可逆性判断替代预测，只在错了也不致命时下注（A · sources/notes/note1.md）
```

`（A）` 也认，但**只写信度不写出处**会被 `quality_check.py` 列为软诊断。
`（A 方案）` 这类正文括号不会被误判为元数据。完整规则见 `references/artifact-format.md` §2.3。

#### Agent prompt 模板

```
你的任务：提炼 [素材/对象] 的 [维度名] 维度。

素材位置：[distilled/<slug>/sources/ 或 指定文件]
[网络补充时] 搜索方向：...

提取重点：
- [该维度的具体提取目标，见 references/schema-*.md]

输出要求：
- 写入 distilled/<slug>/research/0X-<dim>.md
- 每条信息标注信度等级（A 一手直引 / B 一手转述 / C 二手分析 / D 推断）
- 注明出处（文件路径或 URL）
- 发现矛盾直接记录，不要调和
- 区分「素材明说的」vs「我推断的」

信息源黑名单：不使用知乎、微信公众号、百度百科。
```

各维度的具体提取目标见对应的 `references/schema-*.md`。

---

### Phase 2.5: 🔴 CHECKPOINT · 分维度质量确认

展示各维度的条目数与信度分布，用户确认后才进入合成。

维度条目太少（<3 条）或 D 级超标 → 回到 Phase 1 补素材，不要靠推断填满。

---

### Phase 3: 交叉验证

读取 `references/distillation-framework.md` 获取完整方法论。

#### 3.1 三重验证筛选骨架

对每条候选执行：
- **跨域复现**：在 ≥2 个不同领域/章节出现？
- **生成力**：能推断出素材未覆盖的新立场？
- **排他性**：不是所有同类都会这样想？

三重全过 → 核心骨架；过 1-2 重 → 次级条目；0 重 → 丢弃或降为背景。

#### 3.2 信度分级复核

逐条复核 A/B/C/D 标记。**D 级占比 >20% → 说明素材不足**，应标注缺口而不是靠推断填满。

#### 3.3 矛盾分类

| 类型 | 处理 |
|------|------|
| 时间性矛盾（早期 A / 近期 B） | 记录演化轨迹，以近期为主 |
| 领域性矛盾（工作 X / 生活 Y） | 分领域记录，不强求统一 |
| 本质性张力（价值观内在冲突） | 明确列为「核心张力」 |

#### 3.4 缺口标注

列出：素材稀少的维度、从未涉及的关键问题、**素材主体刻意回避的话题**（这本身是重要信息，标注为「主动不公开」而非「信息缺失」）。

---

### Phase 4: 档案合成

按 `references/artifact-format.md` 的契约写入 `DISTILLATE.md`：

| 部分 | 来源 |
|------|------|
| frontmatter | Phase 3 的统计结果（信度分布、蒸馏比、维度、缺口） |
| 一句话 | 骨架的最高浓缩 |
| 核心骨架 | 三重验证通过的条目 |
| schema 专属段 | 见 `schema-*.md` 定义的段落 |
| 表达特征 / 语域 | 表达类维度 |
| 矛盾与张力 | Phase 3.3 |
| 信息缺口 | Phase 3.4 |
| 变更记录 | 本次动作 |

**同步更新 `manifest.json`**：维度状态、coverage、sources、changes。
**未判定的字段写 `null`，不要写 `0`**——`0` 是「判定为没有」，`null` 是「还没判」（铁律 1 在数据层的体现）。

#### 封存档案哈希（必做）

档案正文定稿后**必须**封存哈希，否则下游的漂移检测会静默失效：

```bash
python3 scripts/seal.py distilled/<slug>          # 写入 distillate_sha256
python3 scripts/seal.py distilled/<slug> --check  # 校验是否过期
```

**档案一改，哈希就过期**。`quality_check.py` 会把它当硬性 FAIL 拦截——这不是形式主义：
`distillate_sha256` 是「下游产物是否还基于当前这版档案」的唯一异地依据。

#### 浓度自检

> **每千字档案里，有多少条能通过三重验证的独立条目？**

健康区间 **8-15 条/千字**。低于 5 条 = 水太多，继续去水；高于 20 条 = 没展开，读者理解不了。

---

### Phase 5: 独立质检

**⚠️ 蒸馏 agent 与评分 agent 必须是两个独立 agent，绝不自评自证。**

#### 5.0 先跑静态质检（机械关）

```bash
python3 scripts/quality_check.py distilled/<slug>          # 人读
python3 scripts/quality_check.py distilled/<slug> --json   # 机器读
```

**7 项硬检查**：frontmatter 完整性 / 核心骨架 3-10 条 / 信度标记与 D 级占比 /
矛盾保留与分类（含和稀泥检测）/ 缺口诚实 / 浓度 / manifest 契约与哈希封存。

外加一组**软诊断**：前后端信度分布不一致、骨架缺出处指针、coverage 未回填、
自报字数与压缩比偏差、manifest 与正文 version 不一致等。软诊断是线索不是评分。

静态质检不过 → 先修结构，**别急着进评分卡**。

#### 5.1 再跑独立评分卡

1. 读 `references/quality-scorecard.md`
2. **出题**：3 道生成力题（素材未覆盖但相关）+ 1 道缺口题 + 1 道溯源抽查（随机抽 3 条骨架）
3. **答题 agent**：只读 `distilled/<slug>/` 内文件，禁止联网，禁止补充训练语料知识
4. **评分 agent**：独立 agent，对照原始素材逐维打分
5. 产出 `distilled/<slug>/QUALITY.md`

得分 <70 → 回 Phase 2/3 补维度或补素材。**不要靠加长正文刷分。**

**迭代上限 2 轮**：两轮后仍 <70，在 `QUALITY.md` 与 `gaps` 中如实标注薄弱维度，交付当前最优版本。

#### 记录到评测历史

`QUALITY.md` 每次重跑都被覆盖，**分数历史会丢**。所以每次评分后追加一条到 `EVALS.jsonl`：

```bash
python3 scripts/eval_record.py record --file distilled/<slug>/EVALS.jsonl \
    --json '{...}' --artifact distilled/<slug>
```

记录必须含 `version` / `artifact_sha256` / `models`（答题与评分两个模型）/ `scorers` / **内嵌题目集**。

> **`version` 与 `artifact_sha256` 用 `--artifact` 自动填，不要手抄。**
> 手抄 64 位哈希必然出错，而这个字段是「这次评的到底是哪一版」的唯一依据。

> **题目内嵌在记录里，不做全局题库。** 档案会变，全局固定题库会与之脱节。
> 尤其：缺口补上后，缺口题的正确答案从「拒答」变成「真答」，而评分规则仍把拒答记为正确
> → **越完整的档案分越低**（假退步）。所以缺口填上后要把该题标 `retired`。

---

### Phase 5.5: 对抗精炼

读 `references/adversarial-refine.md`。评分之后加一道对抗环：

| | 攻击者 A | 攻击者 B |
|---|---------|---------|
| distill | **信度攻击者**（无来源断言、D 级伪装 A/B、和稀泥式调和） | **浓度攻击者**（水分、未展开骨架、可合并条目） |

**四条护栏**（缺一不可，否则这轮精炼就是仪式）：

1. **必须交补丁** — before/after 文本 + 指名的 rubric 维度 + 被攻击行号；缺任一项**直接丢弃**
2. **攻击者拿 ground truth** — 喂 `sources/` 与 `manifest.json`。没有它，攻击者只能挑风格，会把每个 A 级断言都当成不可验证而要求**信度通缩**
3. **只许删改，不许新增断言** — 防靠灌水刷浓度分（反模式 #11）
4. **不做「必须提升否则回滚」的评分闸门** — 单次 LLM 评分噪声 ±5–10 分（见两份评分卡的「分差>10分需复核」）。用噪声当闸门，一半概率放过假修复、一半概率回滚真修复

**产出是候选补丁清单**（`REFINE.md`），交用户裁决。质量信号由**第二独立评分 agent** 提供，不是由攻击者提供。

**迭代上限 2 轮**，对齐「宁可交付诚实的 60 分」。

---

### Phase 6: 交付

**🔴 CHECKPOINT · 展示评分卡给用户确认后才算完成。**

交付内容：
- `DISTILLATE.md` — 主档案（人可读）
- `manifest.json` — 素材与维度状态（机器可读，增量蒸馏依据）
- `QUALITY.md` — 质检报告
- `research/` — 分维度底稿（可溯源）

**告知用户下一步**：
- 想直接读 → 打开 `DISTILLATE.md`
- 想当作下游输入 → 档案是自包含的只读产物，格式契约见 `references/artifact-format.md`
- 想增量补充 → 说「补充蒸馏」，走下方流程
- 想复核质量 → 看 `QUALITY.md`；历史分数在 `EVALS.jsonl`

**交付即定型**：`DISTILLATE.md` 的哈希已封存。任何后续改动都必须重新 `seal.py`，
否则下游会认为手里的产物仍与档案同步——而实际上档案已经变了。

---

## 增量蒸馏

当用户说「我又找到一些材料」「补充蒸馏」「更新这份档案」时：

1. 对新素材跑 `python3 scripts/ingest.py <新素材> --out distilled/<slug>/`
2. 比对 `manifest.json` 中已有 `sha256`：
   - **哈希相同** → 跳过（已蒸过）
   - **新素材** → 标记其覆盖的维度
3. **只重跑受影响的维度**，未受影响的维度直接复用现有 `research/` 底稿
4. 更新 `DISTILLATE.md`：新增条目追加，**被新素材推翻的旧条目保留并标注「已被 X 修正」**（不要静默删除——演化轨迹本身是信息）
5. `manifest.json` 的 `changes` 追加记录，`version` +1，`updated` 更新
6. 重算信度分布与蒸馏比；回填 `coverage` 与一手/二手计数（未判定的写 `null`）
7. **重新封存哈希**：`python3 scripts/seal.py distilled/<slug>`（不封存则 `quality_check.py` 报哈希过期）
8. 跑 `python3 scripts/quality_check.py distilled/<slug>` 确认结构未破

**增量蒸馏不重跑全流程。** 这是它与「重新蒸一遍」的核心区别。

---

## 工具与测试

`scripts/` 全部为**纯标准库 Python**（skill 必须自包含，不引入第三方依赖）。

| 脚本 | 职责 | 读写 |
|------|------|------|
| `_lib.py` | **唯一事实源**：契约常量、frontmatter 子集解析、信度统计、表格渲染、哈希 | 库 |
| `ingest.py` | 素材归集；生成/增量更新 `manifest.json` | 写 manifest |
| `seal.py` | 计算并写入 `distillate_sha256`（`--check` 只校验） | 写 manifest |
| `quality_check.py` | 7 项结构检查 + 软诊断（契约的可执行形式） | 只读 |
| `distill_report.py` | Phase 1.5 / 2.5 检查点摘要表 | 只读 |
| `meta_scan.py` | 跨档案：来源重叠 / slug 碰撞 / 候选对照矩阵 / 互补缺口 | 只读 |
| `eval_record.py` | `EVALS.jsonl` 写入 / 历史 / 对比 | 只追加 |

```bash
python3 -m unittest discover -s test -v      # 测试套件
```

**契约同步**：`references/artifact-format.md` 是契约的**权威定义**，`scripts/` 只是它的
**可执行形式**。改字段名 / 枚举 / 目录名 / 检查规则，必须同时改两边——
`test/test_distill.py::TestContractSync` 会检查是否漂移。
`_lib.sha256_file` 的算法是契约的一部分（`distillate_sha256` 靠它），**不要动**——
测试用黄金值钉死了它。

**脚本只做机械活**：解析、统计、渲染、哈希。语义判断（两条主张是否在回答同一个问题、
某维度覆盖度打几分）永远由 agent 做（反模式 #16）。

---

## 失败模式与降级路径

| 触发条件 | 一线修复 | 仍失败兜底 |
|---------|---------|-----------|
| 运行环境不支持并行 subagent | 降级为**串行执行**：做一个维度落盘一个，禁止挂起等待后台通知 | 单 agent 分 N 轮，每轮一个维度并立即落盘 |
| 上下文窗口不足 | 分 Phase 续跑：每个 Phase 结束即落盘，新会话从 `manifest.json` + `research/` 恢复 | 分 3 段会话：Phase 0-1 / 2-3 / 4-6，每段开头先读已落盘文件 |
| 成本失控 | Phase 0 的档位确认就是防线：开跑前报量级 | 用户中途喊停 → 已落盘素材与底稿即可交付的中间产物，续跑不清零 |
| 单个 Agent 超时 | 不等待，继续推进，标注该维度「信息不足」 | 在 `gaps` 中如实记录 |
| 搜索工具不可用 | 改用运行环境可用的等价工具 | 切换为纯本地语料模式，引导用户提供素材 |
| 素材 <10 条 | Phase 0.5 就提醒用户质量受限；骨架减至 3-5 条 | 加厚缺口段，D 级标注，评分卡按实际打分 |
| 素材完全为空 | **停止**，引导用户提供素材 | 绝不凭训练语料生成档案 |
| Agent 结果冲突 | **保留矛盾**——矛盾本身是有价值的信号 | 用「矛盾与张力」段收录 |
| 用户要求蒸在世的非公众人物 | 必须由用户提供素材，并提醒征得当事人同意 | 拒绝纯网络采集，说明隐私边界 |

**关键规则**：宁可交付一个诚实标注了局限的 60 分档案，也不要交付一个看起来完美但实际在编造的 90 分档案。

---

## 品味守则（速查）

| 原则 | 一句话 |
|------|--------|
| 长文 > 金句 | 3000 字长文比 50 条碎片更揭示结构 |
| 争议 > 共识 | 最有争议的部分最能揭示独特性 |
| 变化 > 固定 | 立场改变的地方比一直坚持的更有信息量 |
| 一手 > 二手 | 原文 > 转述 > 分析 > 推断 |
| 重复 ≥3 次 = 真信念 | 反复回到的点才是骨架，漂亮话只是修辞 |
| 具体 > 抽象 | 「他在 X 项目上这么干」> 「他很重视 X」 |

### ❌ 反模式黑名单（绝不做的事）

| # | 反模式 | 为什么 / 替代做法 |
|---|--------|------------------|
| 1 | 把摘要当蒸馏交付 | 按原文顺序每段缩一句是压缩不是蒸馏。必须打散重组为认知结构 |
| 2 | 无来源的断言 | 每条必须能溯源，否则降为 D 级并标注推断 |
| 3 | 和稀泥式调和矛盾 | 「虽然…但是…」抹掉了最有价值的信息。保留并分类 |
| 4 | 编造缺失的维度 | 素材没覆盖就写进 `gaps`，不要用训练语料补 |
| 5 | 素材不足硬蒸 | 素材 <10 条就降档并加厚缺口段，或直接停止 |
| 6 | 用知乎/微信公众号/百度百科做信息源 | 洗稿、失真、无法验证。任何维度都不例外 |
| 7 | 把档案塞进 skill 目录 | 档案属于用户工作区。skill 必须自包含，用户数据不属于 skill |
| 8 | 自评质量 | 蒸馏 agent 不能给自己打分，必须独立 agent 执行评分卡 |
| 9 | 静默删除被推翻的旧条目 | 演化轨迹是信息。标注「已被 X 修正」而非删除 |
| 10 | 不报成本量级直接开跑 | 完整蒸馏是重任务，用户有权先选档位再花钱 |
| 11 | 靠加长正文刷浓度分 | 浓度看的是独立条目数，不是字数。灌水只会稀释 |
| 12 | 把确认检查点变成交付阻塞 | 检查点是让用户纠偏，不是扣住产出。能给默认值就给默认值 |
| 13 | 把来源重叠的档案当独立佐证 | 两份档案共享同一素材时，「两人共识」只是**一份素材数了两次**。必须先跑 `meta_scan.py` 的重叠检测（综合档案） |
| 14 | 把「主动不公开」当成待补的缺口 | 主体刻意回避的话题本身是**信息**，应标注「主动不公开」而非「信息缺失」。把它当缺口去补，就会为刻意沉默的人编造立场——违反 framework §九，评分卡判 0 分 |
| 15 | 用单次评分当精炼闸门 | 单次 LLM 评分噪声 ±5–10 分。用「必须提升否则回滚」当闸门，一半概率放过假修复、一半概率回滚真修复。质量信号应来自第二独立评分 agent |
| 16 | 让脚本做语义判定 | 脚本只做机械抽取（对照矩阵、重叠检测、静态检查）。「这两条是不是在回答同一个问题」「是不是真对立」必须由 agent 读语义判断 |
| 17 | 缺口填上后不退役对应题目 | 缺口题的正确答案会从「拒答」变成「真答」，不标 `retired` 就会得出「越完整分越低」的假退步 |
| 18 | 无证据指针的反馈直接改档案 | 无出处的反馈是**意见**不是缺陷。铁律3 禁止据此行动 |
| 19 | 把「未回填」写成 0 | `0` 是判定结果（确实没有），`null` 是没判。写成 0 会让评分卡的覆盖度规则与 `meta_scan.py` 的互补缺口判定把「没数」当成「数出来是零」。未判定就写 `null`——铁律1 在数据层的体现 |
| 20 | 手抄 `artifact_sha256` / `version` | 64 位哈希手抄必然出错，而它是「这次评的是哪一版」的唯一依据。用 `--artifact` 自动填 |
| 21 | 档案改完不重新封存哈希 | 哈希过期 → 下游会误判「产物仍与档案同步」，而档案其实已经变了。`seal.py` 封存，`quality_check.py` 会硬性拦截 |

---

## 最后

蒸馏的产物不是「这堆材料讲了什么」，而是**「拿这套认知，你能想到什么原来想不到的」**。

一份好档案是一副能戴上的镜片，不是一段更短的原文。
